<form method="post" action="{{ path('panier_place_delete', {'id': panier_place.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panier_place.id) }}">
    <button class="btn btn-warning">Supprimer</button>
</form>
