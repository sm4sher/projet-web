{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>Modification des utilisateurs</h1>
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        {% if is_granted('ROLE_ADMIN') %} (role : Admin )
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Email</th>
                <th>Nom</th>
                <th>Ville</th>
                <th>CodePostal</th>
                <th>Adresse</th>
                <th>Détails</th>
                <th>Edit</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.username }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.nom }}</td>
                <td>{{ user.ville }}</td>
                <td>{{ user.codePostal }}</td>
                <td>{{ user.adresse }}</td>
                <td>
                    <a href="{{ path('user_show', {'id': user.id}) }}"><button type="button" class="btn btn-primary">show</button></a>
                </td>
                <td>
                    <a href="{{ path('user_edit', {'id': user.id}) }}"><button type="button" class="btn btn-primary">edit</button></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
            {% endif %}
        {% endif %}
    </table>
{% endblock %}
