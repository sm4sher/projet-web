<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="/">TP web <span style="color:#26ff1e;">  Menu Client </span></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">

            <!--Commandes de l'utilisateur-->
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="DropdownCommandes" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Commandes
                </a>
                <div class="dropdown-menu" aria-labelledby="DropdownCommandes">
                    <a class="dropdown-item" href="#">Faire une commande(Evenements/panier)</a>
                </div>
            </li>

            <!--Affichage des évenements de l'utilisateur-->
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="DropdownClient" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Evenements
                </a>
                <div class="dropdown-menu" aria-labelledby="DropdownStage">
                <a class="dropdown-item" href="{{ path('evenement_index') }}">Montrer les evenements</a>
                <a class="dropdown-item" href="{#{ path('panier.filtre') }#}">Filtrer par catégorie de evenements</a>
                </div>
            </li>

            <!--Détails du compte : commandes et coordonnées-->
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="DropdownComptes" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    mon compte
                </a>
                <div class="dropdown-menu" aria-labelledby="DropdownComptes">
                    <a class="dropdown-item" href="{{ path('panier_place_index') }}">mes commandes</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('user_show', {'id': app.user.id }) }}">mes coordonnées</a>

                </div>
            </li>
        </ul>

        <ul class="nav navbar-nav navbar-right">
            <!-- Authentication Links -->
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <span class="navbar-text">
                    Bonjour {{ app.user.username }}
                    {% if is_granted('ROLE_ADMIN') %} (role : Admin )
                    {% elseif is_granted('ROLE_CLIENT') %} (role : Client ) 
                    {% endif %}
                </span>
                <a class="btn btn-warning" type="button" href="{{path('logout') }}">
                    Se déconnecter
                </a>

            {% else %}
                <a class="btn btn-success" type="button" href="{{path('login') }}">
                    Se connecter
                </a>
                <a class="btn btn-success" type="button" href="{#{path('changePassword') }#}">
                    mot de passe oublié
                </a>
            {% endif %}
        </ul>
    </div>
</nav>