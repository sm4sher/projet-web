{% extends 'base.html.twig' %}

{% block title %}Categorie index{% endblock %}

{% block body %}
    <h1>Categorie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Détails</th>
                <th>Edit</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.id }}</td>
                <td>{{ categorie.libelle }}</td>
                <td>
                    <a href="{{ path('categorie_show', {'id': categorie.id}) }}">
                        <button type="button" class="btn btn-secondary">show</button>
                    </a>
                </td>
                <td>
                    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}">
                        <button type="button" class="btn btn-secondary">edit</button>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categorie_new') }}"><button type="button" class="btn btn-primary">Create new</button></a>
{% endblock %}
