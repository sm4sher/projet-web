{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}"><button class="btn btn-primary">back to list</button></a>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}"><button class="btn btn-primary">edit</button></a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
